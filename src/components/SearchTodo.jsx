const SearchTodo = () => {
	const handleSearch = () => {

	}
	return (
		<div>
			<input type="search" onChange={handleSearch} placeholder="Search todos..." />
		</div>
	);
};

export default SearchTodo;
